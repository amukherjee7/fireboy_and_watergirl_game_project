import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
public class InstructionTitle extends Actor{
    public InstructionTitle(){
       getImage().scale(getImage().getWidth()/4, getImage().getHeight()/4);
       setImage(getImage());
    }    
}
